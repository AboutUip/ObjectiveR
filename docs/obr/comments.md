# ObjectiveR 注释规范

## 1. 单行注释

- ObjectiveR 的单行注释标记为 `//`。
- 从 `//` 标记开始到该行结束均视为注释内容。

## 2. 多行注释

- ObjectiveR 的多行注释标记为 `/*` 与 `*/`。
- `/*` 与 `*/` 之间的内容均视为注释内容。
- 多行注释可以跨越多行。

## 3. 约束

- 注释类型仅包含且仅包含以上两种：`//` 与 `/* ... */`。
- 任何其它形式的注释标记均不属于本规范定义，必须按语法错误处理。
